- name: import file beat repo file
  ansible.builtin.template:
    src: elk.repo
    dest: /etc/yum.repos.yum

- name: install file beat
  ansible.builtin.yum:
    name: filebeat
    state: installed

- name: copy file beat config file
  ansible.builtin.template:
    src: filebeat.repo
    dest: /etc/filebeat/filebeat.yml

- name: systemd reload
  ansible.builtin.systemd:
    name: filebeat
    state: restarted
    enabled: true
